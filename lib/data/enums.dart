enum SessionStatus {
  success,
  gpsbadstate,
  none,
}

enum GpsPermissionStatus {
  denied,
  waiting,
  successFull,
  deniedForever,
  pure,
  notactivated
}

enum HomePageStatus { loading, success, notSuccess, pure }